use parser::ast::NixExpr;

use super::*;

fn test_ops() {
    // let compiler = Compiler::new();

    // compiler.translate_expression(Code::Op(parser::ast::Op::Binop {
    //     left: (),
    //     right: (),
    //     opcode: (),
    // }))
}
